# Django
DJANGO_SECRET_KEY=
DJANGO_DEBUG=1
DJANGO_ALLOWED_HOSTS=localhost,127.0.0.1

# 32 random bytes, base64-encoded, used as the AES-256-GCM vault master key.
# Generate: python -c "import os,base64;print(base64.b64encode(os.urandom(32)).decode())"
VAULT_MASTER_KEY_BASE64=

# GitHub import
GITHUB_API_BASE_URL=https://api.github.com
GITHUB_TOKEN=

# Fly.io
FLY_API_TOKEN=
FLY_API_BASE_URL=https://api.machines.dev
FLY_ORG_SLUG=personal

# Render
RENDER_API_TOKEN=
RENDER_API_BASE_URL=https://api.render.com
RENDER_OWNER_ID=
RENDER_DEFAULT_REGION=oregon
RENDER_DEFAULT_PLAN=starter

# Supabase
SUPABASE_ACCESS_TOKEN=
SUPABASE_API_BASE_URL=https://api.supabase.com
SUPABASE_ORG_ID=
SUPABASE_DEFAULT_REGION=us-east-1

# Stripe deployment-stage integration for migrated apps
STRIPE_SECRET_KEY=
STRIPE_API_BASE_URL=https://api.stripe.com

# Self-subscription billing for API Transfer
STRIPE_PUBLISHABLE_KEY=
STRIPE_WEBHOOK_SECRET=
STRIPE_PRICE_PRO=
STRIPE_PRICE_SCALE=
BILLING_CURRENCY=usd
BILLING_SUCCESS_URL=http://localhost:8000/billing/success?session_id={CHECKOUT_SESSION_ID}
BILLING_CANCEL_URL=http://localhost:8000/pricing

# Cloudflare DNS and Universal SSL
CLOUDFLARE_API_TOKEN=
CLOUDFLARE_ZONE_ID=
CLOUDFLARE_API_BASE_URL=https://api.cloudflare.com/client/v4
DEPLOY_DNS_TARGET=203.0.113.10

# RBAC (comma-separated API keys per role). Leave empty in dev to default to admin.
RBAC_ADMIN_KEYS=
RBAC_OPERATOR_KEYS=
RBAC_VIEWER_KEYS=
